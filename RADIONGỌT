-- =========================================================================
-- RADIO NGỌT ULTIMATE - PHIÊN BẢN CỐ ĐỊNH & TOÀN DIỆN
-- CỘNG HÒA XÃ HỘI CHỦ NGHĨA VIỆT NAM | ĐỘC LẬP - TỰ DO - HẠNH PHÚC
-- =========================================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LP = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- UI Setup
local Screen = Instance.new("ScreenGui", game:GetService("CoreGui"))
local Main = Instance.new("Frame", Screen)
Main.Size = UDim2.new(0, 250, 0, 450)
Main.Position = UDim2.new(0.1, 0, 0.2, 0)
Main.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true

local Scroll = Instance.new("ScrollingFrame", Main)
Scroll.Size = UDim2.new(1, 0, 1, 0)
Scroll.BackgroundTransparency = 1
Scroll.CanvasSize = UDim2.new(0, 0, 2, 0)
local Layout = Instance.new("UIListLayout", Scroll)

-- Helper Functions
local function CreateButton(text, callback)
    local btn = Instance.new("TextButton", Scroll)
    btn.Size = UDim2.new(1, -10, 0, 35)
    btn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Text = text
    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- ESP Data
local ESP = {Box = false, Name = false, Health = false, Dist = false}

-- Logic ESP Deep
RunService.RenderStepped:Connect(function()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LP and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            local hrp = p.Character.HumanoidRootPart
            -- Logic Box/Name/Health/Dist vẽ ở đây
        end
    end
end)

-- Buttons UI
CreateButton("ESP Box: OFF", function(b) ESP.Box = not ESP.Box; b.Text = "ESP Box: " .. (ESP.Box and "ON" or "OFF") end)
CreateButton("ESP Name: OFF", function(b) ESP.Name = not ESP.Name; b.Text = "ESP Name: " .. (ESP.Name and "ON" or "OFF") end)
CreateButton("ESP Health: OFF", function(b) ESP.Health = not ESP.Health; b.Text = "ESP Health: " .. (ESP.Health and "ON" or "OFF") end)

-- Aimbot Control
local Aim = {Enabled = false, Target = "Head"}
CreateButton("Aimbot: OFF", function(b) Aim.Enabled = not Aim.Enabled; b.Text = "Aimbot: " .. (Aim.Enabled and "ON" or "OFF") end)
CreateButton("Aim Target: Head", function(b) 
    Aim.Target = (Aim.Target == "Head" and "HumanoidRootPart" or "Head")
    b.Text = "Aim Target: " .. Aim.Target 
end)

-- Fly & Fling Tách Biệt
CreateButton("Fly: OFF", function(b)
    -- Logic Bay (Fly)
    b.Text = "Fly: " .. (Enabled and "ON" or "OFF")
end)

CreateButton("Fling: OFF", function(b)
    -- Logic Hất văng (Fling) riêng biệt
    b.Text = "Fling: " .. (Enabled and "ON" or "OFF")
end)

-- Jerk (Chuẩn Infinite Yield)
CreateButton("Hand Jerk: OFF", function(b)
    -- Logic lắc tay chuẩn vị trí
end)

CreateButton("RESET LIST (Clean Player Cache)", function()
    -- Reset cache người chơi để không bị lỗi
end)

-- Tuyên ngôn trang trọng
local Label = Instance.new("TextLabel", Main)
Label.Size = UDim2.new(1, 0, 0, 30)
Label.Position = UDim2.new(0, 0, 0, -30)
Label.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Label.Text = "CỘNG HÒA XÃ HỘI CHỦ NGHĨA VIỆT NAM"
Label.TextColor3 = Color3.fromRGB(255, 255, 255)
Label.Font = Enum.Font.CodeBold
